# 复制为 .env 并填入真实值（.env 不进 git）

# Telegram 推送（可选）
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# 邮件通知（可选）。Gmail 用法：SMTP_HOST=smtp.gmail.com，SMTP_PORT=587，
# SMTP_USER=自己的 Gmail 地址，SMTP_PASSWORD=应用专用密码（不是登录密码），
# EMAIL_TO=收件地址（多个用英文逗号分隔）
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_TO=
